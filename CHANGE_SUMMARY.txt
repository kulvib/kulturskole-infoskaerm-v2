CI PostgreSQL 18 migration-verifier fix

CHANGED:
- backend/scripts/run_migrations.py
  - filters PostgreSQL 18 contype='n' NOT NULL catalog rows from constraint comparison
    because column nullability is already verified via information_schema.columns
  - normalizes the two semantically equivalent PostgreSQL ANY(ARRAY[...]) text-cast renderings
    before exact constraint comparison

ADDED:
- backend/tests/test_migration_catalog_normalization.py
  - proves PG17/PG18 ANY-array renderings normalize equally
  - proves a real membership change still compares unequal
  - locks the PostgreSQL 18 NOT NULL filtering contract

No migrations, ORM models, runtime, systemd units, or frozen-domain implementation changed.
