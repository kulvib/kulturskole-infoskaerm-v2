CI root-terminal enrollment test fixture fix

CHANGED:
- backend/tests/test_admin_entity_audit_contract.py
  Adds deterministic test-only CLIENTFLOW_ROOT_TERMINAL_KEY_B64 and
  CLIENTFLOW_ROOT_TERMINAL_KEY_ID values so the canonical enrollment claim
  can reach the atomicity/audit assertions while production remains fail-closed.

No production/runtime/migration/systemd/domain implementation files changed.
