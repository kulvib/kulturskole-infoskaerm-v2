Step 51B CI Ruff F841 fix

Repo file changed:
- backend/service1/clientflow_update_auth.py

Change:
- Remove the unused local binding `current` while preserving the call to
  active_update_credential(..., for_update=True).
- The call is intentionally retained because its SELECT ... FOR UPDATE row-lock
  side effect serializes provisioning with credential rotation/recovery.

No runtime/auth contract is weakened; no frozen-domain files are changed.
