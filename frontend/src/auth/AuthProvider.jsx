// Canonical auth facade for PlanIQ frontends.
// The implementation lives in authcontext.jsx for backward compatibility.
export { AuthProvider, useAuth } from "./authcontext";
export { AuthProvider as default } from "./authcontext";
